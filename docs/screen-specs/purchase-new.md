# 画面: 発注登録（デモ）

- **画面 ID**: `purchase-new`
- **ルート**: `/purchase/new`
- **初期明細行数**: 10

## ヘッダ

| 項目 | 入力方式 | バリデーション | 備考 |
|------|---------|--------------|------|
| 仕入先コード | マスタ選択（取引先） | 必須 | |
| 備考 | テキスト | — | |

## 明細グリッド

| 列名 | 型 | 編集 | バリデーション | 備考 |
|------|-----|------|--------------|------|
| 行 | 行番号 | × | — | 左固定 |
| 品目コード | テキスト | ○ | 必須（入力行のみ） | |
| 数量 | 数値 | ○ | 1 以上（入力行のみ） | |

## 値確定ルール（副作用）

なし（現時点では副作用ルールは未定義）

## F キー

- F1: 新規（共通デフォルト）
- F12: 保存（共通デフォルト）

画面固有の F キーはなし。

## マスタ・データソース

| 用途 | API | レスポンス形式 | 備考 |
|------|-----|--------------|------|
| 仕入先一覧（ヘッダのプルダウン候補） | GET /api/masters/parties | `[{ code, name }]` | 受注画面と同じ取引先マスタを共用 |

## 画面アクション → API

| アクション | API | ボディ概要 | レスポンス概要 |
|-----------|-----|-----------|--------------|
| 保存（F12） | （未定・モック） | — | — |

## 特記事項

- 品目コードはオートコンプリートなしの単純テキスト入力
- 最終行の最終編集列（数量）で Enter → 新しい行を追加し、その行の品目コードにフォーカス
